Fix Edge browser blocking invoice document previews in the Extraction Review screen.

Current behavior:
The invoice preview uses /api/storage/objects... as the source for the PDF iframe or image tag. Edge is blocking the ingested invoice image/PDF from displaying.

Do not rebuild the app. Only update the document preview/storage response handling.

Review these areas:
- Extraction Review document viewer
- /storage/objects/* route
- object storage download response headers
- PDF/image preview rendering

Requirements:
1. Ensure uploaded invoice PDFs and images are served with correct browser-safe headers.

For /storage/objects/* responses, set or override these headers as appropriate:
- Content-Type should match the actual file type:
  - application/pdf for PDF
  - image/jpeg for JPG/JPEG
  - image/png for PNG
  - image/webp for WEBP
  - image/gif for GIF
  - image/tiff for TIFF
- Content-Disposition should be inline, not attachment.
- X-Content-Type-Options should be nosniff only if Content-Type is correct.
- Do not set X-Frame-Options to DENY or SAMEORIGIN in a way that blocks the app’s own iframe preview.
- Add a Content-Security-Policy that allows the app to display its own stored files in iframe/img preview:
  - frame-src 'self' blob: data:
  - img-src 'self' blob: data: https:
  - object-src 'self' blob:
- If the app uses a global CSP, update it so the invoice viewer is allowed.
- Do not make private invoice files public.
- Keep file access routed through the server-side proxy.
- Do not expose object storage signed URLs directly in the UI.

2. Improve the viewer UI fallback.
- If the iframe/image fails to load, show a clear error message:
  “Document preview blocked or unavailable.”
- Add an “Open document in new tab” button using the same secure /api/storage/objects route.
- Add a “Download original file” button if not already present.
- For PDFs, use iframe first, then provide open/download fallback.
- For images, use img first, then provide open/download fallback.

3. Add URL encoding for object paths.
- Ensure fileObjectPath is safely encoded when building the /api/storage/objects URL.
- Avoid broken URLs when filenames or object paths contain spaces, #, $, %, or other special characters.

4. Preserve security.
- Do not bypass authentication/security controls.
- Do not store invoice files on the local filesystem.
- Do not expose Replit object storage credentials or signed URLs.
- Do not log sensitive invoice contents.

Acceptance criteria:
- Uploaded PDF previews display in Microsoft Edge.
- Uploaded JPG/PNG previews display in Microsoft Edge.
- If Edge still blocks inline preview, user can open the document in a new tab.
- User can download the original invoice.
- The preview still works in Chrome.
- Private object storage remains private.