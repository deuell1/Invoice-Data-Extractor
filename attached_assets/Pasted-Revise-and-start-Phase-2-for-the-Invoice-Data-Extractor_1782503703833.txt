Revise and start Phase 2 for the Invoice Data Extractor app.

Do not build ERP integration in Phase 2. ERP integration is now explicitly deferred to Phase 3 because approval has not been granted yet.

Phase 2 must demonstrate full AP invoice capture functionality using import/export files only.

Do not rebuild the app. Preserve all completed Phase 1 functionality, including upload, extraction, review, vendor matching, duplicate detection, hardened tie-out, approval, VoucherID posting, cleanup, export, KPI counts, and audit logging.

Current Phase 1 status:

* Phase 1 core workflow is complete.
* Tie-out hardening is implemented and UAT passed for executed cases.
* Controlled pilot is approved.
* Remaining Phase 1 exit gates are:

  1. Labeled extraction accuracy measurement.
  2. Manual Microsoft Edge runtime confirmation.
* These remaining items should remain tracked but should not block Phase 2 development in the development environment.
* Do not mark full Phase 1 PASS unless both remaining exit gates are closed.

Phase 2 objective:
Show full invoice capture functionality through controlled import/export file workflows before any ERP integration is approved.

Primary Phase 2 goals:

1. Improve AP operational visibility.
2. Improve exception management.
3. Add invoice search, filtering, and reporting.
4. Add vendor and invoice analytics.
5. Add controlled import/export file workflows.
6. Add export package preparation for future ERP use, without connecting to ERP.
7. Improve auditability and administrative controls.
8. Keep the system internal-pilot safe.

Phase 2 must not build:

* Live ERP integration.
* Automatic ERP posting.
* ERP API connections.
* ERP database connections.
* Automatic payments.
* Automatic vendor creation.
* Full three-way PO matching unless explicitly scoped later.
* GL coding automation unless explicitly scoped later.

Phase 3 placeholder:
Create a clearly marked Phase 3 / Future ERP Integration placeholder only. It may document what would be needed later, but it must not perform any live ERP action.

Phase 2 Feature Set

1. Enhanced Dashboard

Add a Phase 2 dashboard section showing:

* Total invoices uploaded
* Pending extraction count
* Pending review count
* Exception count
* Approved count
* Posted count
* Voided count
* Average extraction confidence
* Average vendor match confidence
* Tie-out failure count
* Tie-out warning count
* Duplicate warning count
* Invoices missing PO
* Invoices missing due date
* Total invoice value by status
* Exception rate percentage
* Export-ready invoice count
* Import validation error count, if import files are supported

Dashboard filters:

* Date range
* Vendor
* Status
* Exception type
* Confidence range
* Tie-out status
* Export status
* Import batch

Keep existing Phase 1 KPI cards working.

2. Exception Management Improvements

Enhance the exception queue so AP users can:

* Filter exceptions by reason
* Filter by validation area:

  * Vendor
  * Duplicate
  * Tie-out
  * PO
  * Missing required field
  * Low confidence
  * Extraction failure
  * Import validation failure
* Sort by age, vendor, amount, confidence, and status
* Add internal notes
* Assign an exception owner
* Mark exception as reviewed
* Return exception to pending approval after correction
* View exception history

Do not remove existing exception behavior.

3. Advanced Invoice Search

Add an invoice search/reporting page or enhance the existing invoice list with:

* Search by vendor name
* Search by VendorID/vendor code
* Search by invoice number
* Search by PO number
* Search by VoucherID
* Search by source file name
* Search by BusinessDocumentID
* Search by export batch ID
* Search by import batch ID
* Filter by status
* Filter by tie-out status
* Filter by validation status
* Filter by date range
* Filter by amount range
* Filter by confidence range
* Filter by source document
* Filter by export status
* Filter by import status

Include clear empty states and loading states.

4. Source Document Management

Improve source document visibility:

* List all uploaded source documents
* Show original file name
* Upload date
* Detected invoice count
* Processing status
* Exception status
* Linked invoices
* Removed/voided status
* Ability to open review for each linked invoice
* Ability to see page ranges for multi-invoice PDFs

Preserve existing source document cleanup and safe file deletion rules.

5. Vendor Analytics

Add a vendor analytics section showing:

* Invoice count by vendor
* Total invoice amount by vendor
* Average vendor match confidence
* Exceptions by vendor
* Duplicate warnings by vendor
* Tie-out failures by vendor
* Invoices missing PO by vendor
* Exported invoice count by vendor
* Invoices by vendor status:

  * active
  * inactive
  * on hold

This is analytics only. Do not create or modify vendors automatically.

6. Import File Workflow

Add controlled import-file functionality for pilot/demo use.

Supported import types:

* Vendor master import
* PO reference import, if currently needed only for validation support
* Invoice correction import, optional only if safe and clearly controlled

Vendor master import requirements:

* Accept CSV file upload.
* Validate required columns before import.
* Required vendor fields:

  * VendorID or VendorCode
  * VendorName
* Optional vendor fields:

  * Aliases
  * PaymentTerms
  * TermsDays
  * IsActive
  * OnHold
* Show preview before committing import.
* Show row-level validation errors.
* Prevent duplicate VendorID/vendor code conflicts unless explicitly updating existing records.
* Do not auto-create vendors from invoice extraction.
* Vendor import must be an admin/import action only.

PO reference import requirements:

* Accept CSV file upload if PO validation reference data is desired.
* Required PO fields:

  * PONumber
  * VendorID or VendorCode, if available
* Optional PO fields:

  * PODate
  * Amount
  * Status
  * Buyer
  * Description
* Use imported PO reference data only for Phase 2 reporting or basic validation.
* Do not build full three-way PO matching in Phase 2.
* If PO reference import is not fully implemented, create the framework and clearly mark PO matching as future scope.

Import batch tracking:
For each import, record:

* Import batch ID
* Import type
* File name
* Uploaded by
* Uploaded timestamp
* Row count
* Rows accepted
* Rows rejected
* Status
* Error summary
* Detailed row-level errors

Import screen requirements:

* Import file upload
* Import type selection
* Template download
* File preview
* Validation result
* Commit/cancel action
* Import history table
* Row-level error report export

7. Export File Workflow

Phase 2 should show full outbound file functionality without ERP integration.

Add export package options for:

* All active invoices
* Approved invoices
* Posted invoices
* Exceptions
* Tie-out failures
* Tie-out warnings
* Vendor summary
* Source document summary
* Export-ready AP invoice file

Export-ready AP invoice file should include:

* DocumentID
* BusinessDocumentID
* VendorID
* VendorNameMatched
* VendorRawName
* InvoiceNumber
* InvoiceDate
* DueDate
* PaymentTerms
* PONumberRaw
* Subtotal
* TaxAmount
* FreightAmount
* DiscountAmount
* OtherChargesAmount
* TotalAmount
* TieOutExpectedTotal
* TieOutDifference
* TieOutStatus
* TieOutExplanation
* Currency
* ExtractionConfidence
* VendorMatchScore
* ValidationStatus
* ExceptionReason
* ReviewStatus
* Status
* FileName
* SourceDocumentID
* InvoiceSequence
* PageStart
* PageEnd
* VoucherID
* CreatedAt
* UpdatedAt
* ExportedAt

Export package requirements:

* Generate CSV.
* Preserve CSV formula-injection protection.
* Exclude voided/removed invoices by default.
* Allow export by status.
* Allow export by date range.
* Allow export by vendor.
* Assign export batch ID.
* Record export history.
* Show who exported, when, what filter was used, and number of records exported.
* Allow re-download of export file if safely stored, or regenerate using saved filter metadata.
* Mark invoices as exported only when export is successful.
* Do not send export files to ERP automatically.

8. File-Based ERP Readiness, Not ERP Integration

Replace the previous ERP integration preparation with file-based readiness tracking only.

Add export readiness fields:

* exportStatus
* exportBatchId
* exportedAt
* exportBlockedReason
* exportRetryCount, only if export generation can fail and be retried
* exportFileName
* exportFormat

Add a clear “Export Ready” indicator when:

* invoice is approved or posted according to current workflow rules
* vendor is matched
* invoice number exists
* total amount exists
* tie-out is not FAIL
* duplicate check is not FAIL
* invoice is not voided/removed

Do not use the terms “ERP posted” or “ERP synced” in Phase 2 UI unless clearly marked as future Phase 3.

Use:

* Export Ready
* Exported
* Export Failed
* Export Blocked

Do not use:

* ERP Posted
* ERP Synced
* Sent to ERP

9. Extraction Accuracy Tracking Framework

Use the existing accuracy harness and add a UI or admin-facing reporting area to record accuracy results from labeled test packs.

Support:

* Accuracy run date
* Test pack name
* Number of invoices tested
* Total fields tested
* Correct fields
* Incorrect fields
* Missing fields
* Overall accuracy percentage
* Accuracy by category:

  * vendor
  * invoice number
  * dates
  * amounts
  * PO
  * currency
* PASS/FAIL against threshold
* Link or reference to generated report

Do not fake accuracy results. If no labeled pack exists, display “Not measured.”

10. Audit and Activity Improvements

Enhance audit visibility:

* Show audit trail by invoice
* Show audit trail by source document if available
* Include user/action/time/field/old value/new value
* Include validation reruns
* Include extraction reruns
* Include vendor rematches
* Include approval and VoucherID events
* Include void/delete actions
* Include import file events
* Include export file events

Keep existing audit entries intact.

11. Internal Admin Settings

Add a simple internal settings area for configurable thresholds:

* Extraction confidence threshold
* Vendor match threshold
* Tie-out pass tolerance
* Tie-out warning tolerance
* Default invoice list page size
* Default export status
* Default export format

Settings may be stored in database or configuration, but must have safe defaults matching Phase 1:

* Extraction confidence threshold: 85%
* Vendor match threshold: 85%
* Tie-out pass tolerance: $0.01
* Tie-out warning tolerance: $0.05

Do not expose secrets in the UI.
Do not expose the OpenAI API key.

12. Phase 2 Regression Protection

After Phase 2 changes, verify that these Phase 1 workflows still work:

* Upload invoice
* Extract invoice
* Review invoice
* Edit fields
* Vendor matching
* Vendor autocomplete
* Duplicate detection
* Tie-out PASS/WARNING/FAIL/SKIPPED
* Exception routing
* Approval workflow
* VoucherID posting
* Posted invoice immutability
* Void/remove/delete
* CSV export
* KPI dashboard
* Source document handling
* Multi-invoice PDF handling
* Audit logging

Phase 2 Acceptance Criteria

Phase 2 is acceptable only if:

* No Phase 1 workflow is broken.
* Dashboard metrics match database ground truth.
* Exception filters work.
* Invoice search and filters work.
* Source document list accurately links to invoices.
* Vendor analytics are read-only and accurate.
* Import file workflow validates files before commit.
* Import file workflow provides row-level error reporting.
* Export file workflow generates accurate CSV files.
* Export file workflow records export batch history.
* Export-ready status is accurate.
* Accuracy reporting framework does not invent results.
* Audit history remains intact.
* CSV exports include required fields and formula-injection protection.
* No live ERP integration is built.
* No automatic ERP posting is built.
* No automatic payment workflow is built.
* Admin threshold settings have safe defaults.
* No critical or high defects are open.

Deliverables

After implementation, provide a Phase 2 status report with:

1. Executive Summary

* What was added
* What was not added
* Confirmation that ERP integration was deferred to Phase 3
* Current project status
* Whether Phase 2 is ready for UAT

2. Feature Checklist

* Dashboard
* Exception management
* Invoice search
* Source document management
* Vendor analytics
* Import file workflow
* Export file workflow
* Export readiness tracking
* Accuracy tracking
* Audit improvements
* Admin settings
* Regression protection

3. Data Model Changes
   List all new tables, fields, or settings.

4. API Changes
   List new or changed API endpoints.

5. UI Changes
   List new or changed screens/components.

6. Import/Export File Documentation
   Document:

* Supported import templates
* Required columns
* Optional columns
* Validation rules
* Export formats
* Export columns
* Export batch behavior

7. Regression Results
   Confirm Phase 1 workflows still work.

8. Known Risks
   Include:

* Labeled extraction accuracy still open if no test pack exists
* Edge runtime confirmation still open if not manually verified
* ERP integration intentionally deferred to Phase 3
* Authentication remains internal-pilot only unless implemented separately

9. Recommended Next Step
   Recommend whether to begin Phase 2 UAT, continue development, or fix blockers.

Important:
Phase 2 must show full functionality using import/export files only.
ERP integration is Phase 3 and must not be implemented until approval is granted.
