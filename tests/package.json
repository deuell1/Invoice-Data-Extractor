{
  "name": "@workspace/tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test ../uat/extraction-accuracy/preflight-check.test.mjs ../uat/extraction-accuracy/preflight-exit.test.mjs && playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
