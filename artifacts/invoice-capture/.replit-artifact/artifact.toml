kind = "web"
previewPath = "/"
title = "Invoice Capture MVP"
version = "1.0.0"
id = "artifacts/invoice-capture"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20658

[services.development]
run = "pnpm --filter @workspace/invoice-capture run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/invoice-capture", "run", "build" ]
publicDir = "artifacts/invoice-capture/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20658"
BASE_PATH = "/"
